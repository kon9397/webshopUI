export interface Product {
    category_id: number,
    created_at: string,
    description: string,
    image_url: string,
    name: string,
    price: number,
    updated_at: string,
    year: number
}